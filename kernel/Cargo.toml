[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
scheduler-stress = []

[[bin]]
name = "kernel"
path = "src/main.rs"

[dependencies]
common = { path = "../common" }
